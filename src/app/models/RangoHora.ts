export class RangoHora {
    id: number
    desde: number
    hasta: number
    constructor(id: number, desde: number, hasta: number) {
        this.id = id
        this.desde = desde
        this.hasta = hasta
    }
}